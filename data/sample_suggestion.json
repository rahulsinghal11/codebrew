{
  "suggestions": [
    {
      "id": 1,
      "type": "performance",
      "title": "Use list comprehension",
      "description": "Replace for-loop with list comprehension for better performance",
      "severity": "low",
      "example": {
        "before": "result = []\nfor x in range(10):\n    result.append(x*2)",
        "after": "result = [x*2 for x in range(10)]"
      }
    },
    {
      "id": 2,
      "type": "security",
      "title": "Secure password handling",
      "description": "Use secure password hashing instead of plain text storage",
      "severity": "high",
      "example": {
        "before": "password = user_input",
        "after": "password = hashlib.sha256(user_input.encode()).hexdigest()"
      }
    }
  ]
} 